/**
 * Each message being shared between processors is classified as TERMINATE or IDENTIFIER message
 * 
 * @author Mayuri Wadkar, Eric Han, Sonali Mishra
 *
 */
public enum MessageType {
	TERMINATE, IDENTIFIER
}
